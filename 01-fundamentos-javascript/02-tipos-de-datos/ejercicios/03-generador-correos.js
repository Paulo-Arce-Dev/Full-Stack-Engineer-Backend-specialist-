// Ejercicio - El Generador de Correos (Manipulación de Strings)

let nombre = "Lucia";
let apellido = "Garcia";

const correoInstitucional = `${nombre[0]}${apellido}@tesla.com`.toLowerCase();
console.log(correoInstitucional);
